$OpenBSD$
--- src/Engine/CrossPlatform.cpp.orig	Tue Nov  1 06:36:42 2011
+++ src/Engine/CrossPlatform.cpp	Tue Nov  1 06:40:16 2011
@@ -131,6 +131,7 @@ void showError(const std::wstring &error)
  */
 std::string findDataFolder(bool exists)
 {
+    exists = true;
 #ifdef _WIN32
 	char path[MAX_PATH];
 
@@ -196,6 +197,7 @@ std::string findDataFolder(bool exists)
  */
 std::string findUserFolder(bool exists)
 {
+    exists = true;
 #ifdef _WIN32
 	char path[MAX_PATH];
 
