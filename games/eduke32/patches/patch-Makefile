$OpenBSD$
--- Makefile.orig	Fri Mar  4 10:50:58 2011
+++ Makefile	Wed May  4 16:41:17 2011
@@ -56,7 +56,11 @@ ifeq ($(PLATFORM),WINDOWS)
     EOBJ=eobj_win
     LIBS+= $(L_SSP) -Wl,--enable-auto-import
 else
-    LIBS+= -ldl -pthread
+    ifeq ($(PLATFORM),BSD)
+        LIBS+= -pthread
+    else
+        LIBS+= -ldl -pthread
+    endif
     ifneq (0,$(PROFILER))
         LIBS+= -lprofiler
         debug+= -pg
