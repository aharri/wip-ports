$OpenBSD$
--- build/Makefile.shared.orig	Thu Apr  7 04:16:29 2011
+++ build/Makefile.shared	Wed May  4 15:47:50 2011
@@ -110,6 +110,16 @@ endif
 ifeq ($(PLATFORM),BSD)
     RENDERTYPE=SDL
     BUILDCFLAGS+= -DHAVE_INTTYPES
+    ifeq ($(findstring i386, $(shell uname -m)), i386)
+        NOASM=0
+    else
+        override NOASM=1
+    endif
+    GTKCOMPAT32=0
+    WITHOUT_GTK=1
+    HAVE_GTK2=0
+    NEDMALLOC=0
+    STDCPPLIB=-lstdc++
 endif
 ifeq ($(PLATFORM),BEOS)
     RENDERTYPE=SDL
